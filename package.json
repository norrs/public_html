{
  "name": "simple-markdown-blog",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "prepare-history": "python scripts/generate_history.py",
    "dev": "bash scripts/dev.sh",
    "build": "npm run prepare-history && hugo --minify"
  },
  "dependencies": {
    "@openai/codex": "^0.55.0",
    "npm": "^10.9.4"
  }
}
